# `quinjet project`

`quinjet project` exposes Quinjet's recent-project index to native clients and
scripts. It does not require a current repository.

## At a glance

| Command | What it does |
| --- | --- |
| `quinjet project list` | Prints recent projects with every linked worktree. |

## Commands

- [`quinjet project list`](./list.md)
